# lazy.nvim のロックファイルはマシンごとに変動するため chezmoi 管理外にする
.config/nvim/lazy-lock.json

# gh CLI の認証情報（トークンを含み得る）は chezmoi 管理外にする
.config/gh/hosts.yml

# 設定検証用テストはホームへ展開しない
tests/

# 1Password連携のセットアップスクリプトはホームへ展開しない
scripts/


# 開発用の git worktree はホームへ展開しない
worktrees/
